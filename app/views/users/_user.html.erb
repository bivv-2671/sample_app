<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if check_admin? user %>
    <%= link_to t("layouts.header.delete"), user, method: :delete, data: {confirm: t("layouts.header.are_u_sure") } %>
  <% end %>
</li>
